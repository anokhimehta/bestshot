apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: bestshot-platform
spec:
  selector:
    app: mlflow
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30500