# THIS IS AN EXAMPLE FILE — DO NOT PUT REAL VALUES HERE
# To create the actual secret run this command on your master node:
#
# kubectl create secret generic immich-db-secret \
#   --from-literal=DB_PASSWORD=REPLACE_WITH_REAL_PASSWORD \
#   --from-literal=DB_USERNAME=immich \
#   --from-literal=DB_DATABASE_NAME=immich \
#   -n bestshot-app

apiVersion: v1
kind: Secret
metadata:
  name: immich-db-secret
  namespace: bestshot-app
type: Opaque
stringData:
  DB_PASSWORD: REPLACE_ME
  DB_USERNAME: immich
  DB_DATABASE_NAME: immich