FROM rocm/pytorch:rocm6.2_ubuntu22.04_py3.10_pytorch_release_2.3.0

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    onnx \
    pillow \
    numpy \
    requests \
    python-swiftclient \
    keystoneauth1 \
    python-keystoneclient \
    python-dotenv \
    prometheus-fastapi-instrumentator \
    prometheus-client \
    timm \
    opencv-python-headless \
    onnxruntime-rocm \
    mlflow \
    lightning

# force reinstall ROCm torch AFTER mlflow/lightning to override any CPU torch they installed
RUN pip install --force-reinstall --no-cache-dir \
    torch torchvision \
    --index-url https://download.pytorch.org/whl/rocm6.2

COPY app.py .
COPY model.py .
COPY benchmark.py .
COPY testing_example_input.json .
COPY config.py .
COPY model.onnx .
COPY images/ ./images/
COPY configs/ ./configs/
COPY sidecar.py .
COPY test_sidecar.py .

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]